{
  "name": "blogging-platform",
  "version": "1.0.0",
  "description": "Full-stack MERN blogging platform with authentication and admin portal",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node server.js",
    "client": "cd client && npm start",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["blogging", "mern", "react", "nodejs"],
  "author": "nomita1303",
  "license": "MIT"
}
